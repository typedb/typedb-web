import { makeStyles } from '@material-ui/core/styles';

export const cloudPageStyles = makeStyles({
    defaultSubsection: {
        maxWidth: '1160px',
        margin: '0 auto',
        marginTop: '80px',
    },
});
