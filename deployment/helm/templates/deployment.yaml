apiVersion: apps/v1
kind: Deployment
metadata:
  name: typedb-web
spec:
  replicas: 0
  selector:
    matchLabels:
      app: typedb-web
  template:
    metadata:
      name: typedb-web
      labels:
        app: typedb-web
    spec:
      containers:
        - name: typedb-web
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: 80
              name: http
              protocol: TCP
          workingDir: /opt/typedb-web
          command: [
            ./server,
            --address={{ .Values.address }},
          ]
      imagePullSecrets:
        - name: docker-secret
